import React from 'react';
import { DriverFormExample } from '@/components/ui/driver-form-example';

/**
 * Demo Page - Driver Form Card Component
 * 
 * Para ver esta página en tu aplicación, agrega esta ruta a tu router:
 * 
 * import { DriverFormDemo } from '@/pages/DriverFormDemo';
 * 
 * Y luego agrega la ruta en tu configuración de rutas.
 */
export default function DriverFormDemo() {
  return <DriverFormExample />;
}
